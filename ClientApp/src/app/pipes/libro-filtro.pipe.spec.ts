import { LibroFiltroPipe } from './libro-filtro.pipe';

describe('LibroFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new LibroFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
